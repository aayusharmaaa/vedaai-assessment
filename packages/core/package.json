{
  "name": "@veda/core",
  "version": "1.0.0",
  "private": true,
  "description": "Assessment engine: question/answer extraction, mapping and grading. No UI dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/test-mapping.ts tests/test-gemini.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
